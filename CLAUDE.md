# exit66jukebox

Read `AGENTS.md` at the repo root. It is the single source of instructions for
every agent working here — build and test commands, the traps that masquerade as
flaky tests, the frontend test hazard, and issue labelling.

Nothing repo-specific belongs in this file. Two files describing one repo drift,
and the stale one is believed.
